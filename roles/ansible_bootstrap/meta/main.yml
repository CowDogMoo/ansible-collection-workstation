---
galaxy_info:
  role_name: ansible_bootstrap
  author: Jayson Grace
  namespace: cowdogmoo
  description: Templates ~/.ansible.cfg and creates the ~/.ansible directory tree (collections, roles, fact_cache) plus the remote tmp directory.
  company: CowDogMoo
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: Kali
      versions:
        - all
    - name: EL
      versions:
        - all
    - name: macOS
      versions:
        - all
  galaxy_tags:
    - ansible
    - bootstrap
    - config
dependencies: []
